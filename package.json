{
  "name": "backend-modul-project",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev:frontend": "npm run dev --workspace=frontend",
    "dev:backend": "npm run dev --workspace=backend",
    "dev:world-drive": "npm run dev --workspace=service-world-drive",
    "dev": "npm run dev:backend & npm run dev:world-drive & npm run dev:frontend",
    "simulate:world-drive": "npm run simulate --workspace=service-world-drive",
    "build:frontend": "npm run build --workspace=frontend",
    "build:backend": "npm run build --workspace=backend",
    "build:world-drive": "npm run build --workspace=service-world-drive",
    "build": "npm run build:backend && npm run build:world-drive && npm run build:frontend"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}